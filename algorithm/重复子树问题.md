# [重复子树问题](https://leetcode-cn.com/problems/find-duplicate-subtrees)

给定一棵二叉树，返回所有重复的子树。对于同一类的重复子树，你只需要返回其中任意一棵的根结点即可。

两棵树重复是指它们具有相同的结构以及相同的结点值。

示例 1：

        1
       / \
      2   3
     /   / \
    4   2   4
       /
      4
下面是两个重复的子树：

      2
     /
    4
和

    4

因此，你需要以列表的形式返回上述重复子树的根结点。

#### 我的代码

```java
import java.util.ArrayList;
import java.util.HashMap;
import java.util.List;

//Definition for a binary tree node.
class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;

    TreeNode() {
    }

    TreeNode(int val) {
        this.val = val;
    }

    TreeNode(int val, TreeNode left, TreeNode right) {
        this.val = val;
        this.left = left;
        this.right = right;
    }
}

class Solution {
    HashMap<String,Integer> list = new HashMap<>();
    List<TreeNode> list2 = new ArrayList<>();

    public List<TreeNode> findDuplicateSubtrees(TreeNode root) {
        if (root == null)
            return null;
        int num;
        findDuplicateSubtrees(root.left);
        String str = traverse(root);
        if (list.containsKey(str)) {
            num = list.get(str);
            list.remove(str);
            list.put(str, num+1);
            if(num == 1) list2.add(root);
        } else {
            list.put(str, 1);
        }
        findDuplicateSubtrees(root.right);
        return this.list2;
    }

    // 序列化
    String traverse(TreeNode root) {
        if (root == null)
            return "#";
        return root.val + "," + traverse(root.left) + "," + traverse(root.right);
    }
}
```

问题：

1. 序列化的返回结果，需要逗号隔开，且不能是中序

2. 复杂度太高

3. HashMap常用函数：

   ```java
   put(key,value);//添加一个键值对，返回值，若键已存在，则覆盖（修改）
   remove(key);//删除，返回删除的value值
   replace(key,value);//将指定的key对应的value值替换为给定的value值，返回替换掉的值
   get(key);
   keySet();//返回集合中所有key的set集合
   entrySet();//返回集合中所有键值对的entry实例对象的集合(Set<Entry<key,value>)
   // foreach 运行效率高
   for (Map.Entry<String, String> me:hm1.entrySet()) {
   			System.out.println(me.getKey()+":"+me.getValue());
   		}
   //iterator 迭代器
   Set<Entry<String, String>> se = hm1.entrySet();
   Iterator<Entry<String, String>> it = se.iterator();
   while(it.hasNext()){
   	Map.Entry me =  it.next();
   	System.out.println(me.getKey()+":"+me.getValue());
   }
   
   ```

   

#### 正确解法

```java
// 记录所有子树以及出现的次数
HashMap<String, Integer> memo = new HashMap<>();
// 记录重复的子树根节点
LinkedList<TreeNode> res = new LinkedList<>();

/* 主函数 */
List<TreeNode> findDuplicateSubtrees(TreeNode root) {
    traverse(root);
    return res;
}

/* 辅助函数 */
String traverse(TreeNode root) {
    if (root == null) {
        return "#";
    }

    String left = traverse(root.left);
    String right = traverse(root.right);

    String subTree = left + "," + right+ "," + root.val;

    int freq = memo.getOrDefault(subTree, 0); 
    // 多次重复也只会被加入结果集一次
    if (freq == 1) {
        res.add(root);
    }
    // 给子树对应的出现次数加一
    memo.put(subTree, freq + 1);
    return subTree;
}
```

